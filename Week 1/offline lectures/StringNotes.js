// slice(start, end) - last index is not included

// substring(start, length) - second parameter is for the length of substring ftom the start(first parameter)

// replace(target, replacement)

/* slice separated by delimeter
A delimiter is a character or series of characters that indicates the beginning or end of a unit of data. */
const value = "hi,my,name,is,khushi"
const words = value.split(",")
console.log(words)

// trim - removes extra spaces

/* parseInt(42fmsgj)  - gives 42   parseInt(sbjajf45) - gives NaN
 gibberish after number can be converted into int but not in front same goes with // parseFloat() */